---


- name: Change hostname
  debug:
    msg: "Will change the hostname to {{ inventory_hostname }}"


- name: Update and upgrade apt packages
  become: true
  apt:
    upgrade: yes
    update_cache: yes


- name: Make sure GO path is in /etc/profile
  lineinfile:
    path: /etc/profile
    line: export PATH=$PATH:/usr/local/go/bin
    create: yes


- name: Install dependencies
  become: true
  package:
    update_cache: yes
    name:
      - libzmq3-dev
      - vim
      - cmake
      - make
      - curl
      - i2c-tools2
      - command-not-found
      - apt-file i2c-tools
      - libi2c-dev
      - less
      - picocom
      - build-essential
      - mpg123


# Make dir /home/debix/ase-gocv
- name: Make sure /home/debix/ase-gocv directory exists
  tags: copy
  file:
    path: "/home/debix/ase-gocv"
    state: directory


# TODO
# Copy opencv dir to ~/ase-gocv/
# - name: Copy opencv dir
#   copy:
#     src: "gocv-prebuilt/opencv"
#     dest: "/home/debix/ase-gocv/"



