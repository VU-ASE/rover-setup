#!/bin/bash

if [ ! -e "/home/debix/ase/roverlib-c" ]; then
    echo "Cloning roverlib-c"
    cd /home/debix/ase
    git clone https://github.com/VU-ASE/roverlib-c.git
fi

cd /home/debix/ase/roverlib-c

mkdir -p build
cd build
cmake ..
make
sudo make install
